# 行舟影视协作约定

- 用户要求：每次软件更新都要发布新版本，确保用户能在软件内更新。完成修改和验证后更新版本及 `release-notes/<version>.md`，提交源码，制作并发布安装包，核对公开 `latest.json` 的版本、下载地址、文件大小和 SHA256。
- 发布命令：`npm run release`（测试、构建、打包、安装包运行时验证、GitHub 发布及更新清单）。若已完成打包验证，可使用 `python scripts/publish_release.py` 发布现有对应版本的安装包。
- 验证命令：`npm test`、`npm run build`。涉及页面交互的更新另做界面验证；涉及付费生成的验证使用模拟接口，除非任务明确需要实际付费生成。
- 不记录或输出 API Key、GitHub 凭据、云端密钥等秘密。保留用户已有工作，不以绕过项目权限验证来修复权限错误。
